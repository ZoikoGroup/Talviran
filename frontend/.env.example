VITE_API_BASE_URL=http://localhost:8000
# Set to "true" to use the offline mock reply generator (data/mockReply.ts)
# instead of the real backend. Never set this in a production build.
VITE_USE_MOCK=false
